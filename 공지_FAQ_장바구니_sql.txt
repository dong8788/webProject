-- 공지
select * from NOTICE where NOTICESEQ = #{seq}

insert into NOTICE("NOTICESEQ", "NOTICECATEGORY", "CONTENT", "TITLE", "REGDATE")
values("NOTICE_SEQ".nextval, #{category}, #{content}, #{title}, SYSDATE)

delete from NOTICE where NOTICESEQ = #{seq}

update NOTICE set
	NOTICECATEGORY = #{category},
	CONTENT = #{content},
	TITLE = #{title},
	REGDATE = SYSDATE


-- FAQ
select * from FAQ where FAQSEQ = #{seq}

insert into FAQ("FAQSEQ", "FAQCATEGORY", "QUERY", "REPLY", "REGDATE")
values("FAQ_SEQ".nextval, #{category}, #{query}, #{reply}, SYSDATE)

delete from FAQ where FAQSEQ = #{seq}

update FAQ set
	FAQCATEGORY = #{category},
	QUERY = #{query},
	REPLY = #{reply},
	REGDATE = SYSDATE


-- 장바구니
select * from CART where CUSSEQ = #{seq}

insert into CART("CUSSEQ", "PRODSEQ", "POQUANTITY")
values("CART_SEQ".nextval, #{}, #{})

delete from CART where CUSSEQ = #{seq}

update CART set
	PRODSEQ = #{},
	POQUANTITY = #{}
